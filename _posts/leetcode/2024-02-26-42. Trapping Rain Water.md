---
title: 42 Trapping Rain Water Level Hard - Java
categories: leetcode
---

## 목차
- [문제](#문제)
- [풀이](#풀이)
  - [완전탐색 접근](#완전탐색-접근)
  - [누적합 접근](#누적합-접근)
  - [투포인터 접근](#투포인터-접근)
  - [스택 접근](#스택-접근)
- [알고리즘 특징](#알고리즘-특징)
- [시간복잡도](#시간복잡도)
- [코드](#코드)

# 문제
**벽 사이에 고인 물을 계산하는 문제**  
-> 최소 높이와 최대 높이 사이에 쌓인 물의 부피를 구하는 것이 핵심  

# 풀이
![다운로드 (14)](https://github.com/user-attachments/assets/b29437ff-0919-4d06-9666-6e6543aa53c6)  
**문제 접근 방식**  
* 최소 높이 기준 시작  
* 최소 높이보다 낮다면 물이 쌓였다는 이야기이기 때문에 값을 누적한다.  
* 최소 높이보다 높다면 최대높이고, 최대높이는 다시 메커니즘 시작지점에서 다시 계산을 시작한다.  

![다운로드 (15)](https://github.com/user-attachments/assets/da9cf499-f545-485f-947b-7eac05e66484)  

**문제 이슈**  
ex 3 2 1 2 1 블럭 형태가 있을 때 고인 물은 1개지만, 6개의 계산 결과가 나온다.  
반드시 최소 높이와 최대높이 사이에서 조건문에 따라 물의 높이를 구해야 한다.  

## 완전탐색 접근
![다운로드 (16)](https://github.com/user-attachments/assets/90f22f55-259c-41be-8725-0bf87dd5421a)  
반복문 i를 돌릴 때 매번 i의 위치에서 최소높이와 최대높이를 구하게 된다면, 문제를 무식하게 풀 수 있다.  
![다운로드 (17)](https://github.com/user-attachments/assets/027b9016-df4a-4e37-a182-48525c12e7bc)  

## 누적합 접근
![다운로드 (18)](https://github.com/user-attachments/assets/fa988355-1d5a-4ea3-8dbe-96f4cea74184)  
![다운로드 (19)](https://github.com/user-attachments/assets/9909448c-028a-4fd8-8724-b830fd17ba66)  
![다운로드 (20)](https://github.com/user-attachments/assets/f1f0d550-993a-4c5e-8409-8bba1742f2d9)  

접근 idx 물리적 시점에서 leftMax와 RightMax를 구해서 푸는 방법이다.  
* 0번 idx 관점에서 최소높이는 0이고 최대높이는 3이다.  
-> min(0, 3) - height[0]  = 01번 idx 관점에서 최소높이는 1이고 최대높이는 3이다.  
-> min(1, 3) - height[1]  = 0  
* 2번 idx 관점에서 최소높이는 1이고 최대높이는 3이다.  
-> min(1, 3) - height[2]  = 1  
...  
9번 idx 관점에서 최소높이는 3이고 최대높이는 2이다.  
-> min(3, 2) - height[9]  = 0  

## 투포인터 접근
![다운로드 (21)](https://github.com/user-attachments/assets/4295d6e3-ce9c-45ab-839b-efece18cd6f7)  
두 포인터를 최소 높이와 최대 높이 기준에 따라 움직이며 물의 부피를 구하는 방법이다.  

## 스택 접근
접근 방식 중 가장 참신하면서도 스택의 활용을 가장 잘한~ 방식이 아닐까 생각한다.  
![다운로드 (22)](https://github.com/user-attachments/assets/7fc45e26-e31f-4bc7-bad5-b4f49345bc6e)  
![다운로드 (23)](https://github.com/user-attachments/assets/f814328c-aadd-4e22-8e92-12ac48184a3c)  
![다운로드 (25)](https://github.com/user-attachments/assets/824a5160-3338-4c3b-b1ad-7daa39749c6e)  
distance에 마지막 1을 빼주는 이유는 0번부터 idx가 시작하기 때문이다.  
distance * water는 해당 길이의 물의 높이가 들어가야 하기 때문이다.  

# 알고리즘 특징
* Bruteforce  
* Prefix Sum  
* TwoPointer  
* Stack  

# 시간복잡도
* Bruteforce #1 - o(n^2)  
* Prefix Sum #2 - o(n)  
* TwoPointer #3 - o(n)  
* Stack #4 - o(n^2)  

# 코드  
```
class Solution {
    // Bruteforce
    public int trap(int[] height) {
        int answer = 0;
        
        for(int i = 0; i < height.length; i++) {
            final int currH = height[i];
            int lMax = currH;
            int rMax = currH;
            
            for(int j = i - 1; j >= 0; j--) {
                lMax = Math.max(lMax, height[j]);
            }

            for(int k = i + 1; k < height.length; k++) {
                rMax = Math.max(rMax, height[k]);
            }

            final int waterVol = Math.min(lMax, rMax) - currH;
            answer += waterVol;
        }

        return answer;
    }
}
```
```
// Prefix Sum
    public int trap(int[] height) {
        int answer = 0;
        int lMaxArr[] = new int[height.length];
        int rMaxArr[] = new int[height.length];

        lMaxArr[0] = height[0];
        for(int i = 1; i < height.length; i++) {
            lMaxArr[i] = Math.max(height[i], lMaxArr[i - 1]);
        }

        rMaxArr[height.length - 1] = height[height.length - 1];
        for(int i = height.length - 2; i >= 0; i--) {
            rMaxArr[i] = Math.max(height[i], rMaxArr[i + 1]);
        }

        for(int i = 0; i < height.length; i++) {
            final int waterVol = Math.min(lMaxArr[i], rMaxArr[i]) - height[i];
            answer += waterVol;
        }

        return answer;
    }
```
```
// twoPointer
    public int trap(int[] height) {
        int l = 0;
        int r = height.length - 1;
        int lMax = height[l];
        int rMax = height[r];
        int answer = 0;

        while(l < r) {
            if (lMax <= rMax) {
                final int wV = lMax - height[l];
                answer += wV;
                
                lMax = Math.max(lMax, height[++l]);
            } else {
                final int wV = rMax - height[r];
                answer += wV;

                rMax = Math.max(rMax, height[--r]);
            }
        }

        return answer;
    }
```
```
// Stack
    public int trap(int[] height) {
        Stack<Integer> s = new Stack<>();
        int answer = 0;

        for(int i = 0; i < height.length; i++) {
            final int currH = height[i];
            while(!s.isEmpty() && height[s.peek()] < currH) {
                final int ele = s.pop();
                if (s.isEmpty())
                    break;

                final int distance = i - s.peek() - 1;
                final int waterV = Math.min(height[s.peek()], currH) - height[ele];
                answer += waterV * distance;
            }

            s.push(i);
        }

        return answer;
    }
```