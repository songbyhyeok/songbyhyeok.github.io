---
title: 21 Merge Two Sorted Lists Level Easy - Java
categories: leetcode
---

## 목차
- [문제](#문제)
- [풀이](#풀이)
  - [1. dummy 노드에 두 노드를 병합하는 방식](#1-dummy-노드에-두-노드를-병합하는-방식)
  - [2. 재귀 특성을 이용한 병합 방식](#2-재귀-특성을-이용한-병합-방식)
- [코드](#코드)

# 문제
[https://leetcode.com/problems/merge-two-sorted-lists/description/](https://leetcode.com/problems/merge-two-sorted-lists/description/)  
  
**두 개의 오름차순으로 정렬된 연결 리스트를 병합하는 문제**  

# 풀이
## 1. dummy 노드에 두 노드를 병합하는 방식
![다운로드 (31)](https://github.com/user-attachments/assets/eaeedad2-165c-491b-bbe0-a06e8247485f)  
두 노드를 오름차순으로 병합하는 방법은 이해하기 쉽지만, 구현하는 과정을 표현하기는 어렵다. 노드는 포인터이므로 자바의 원시값 복사와는 다르게 동작하기 때문이다.  
  
**노드 생성과 값 복사 방식**  
![다운로드 (34)](https://github.com/user-attachments/assets/0f916cbf-9ace-4118-98ef-9ec045d3854c)  
처음에는 두 노드를 비교해서 값을 새 노드를 생성해 넣는 방식으로 접근하여 풀게 되었다. 그러나 새로운 개선 접근 방법을 보고 나서 깨달은 점은 연속적인 노드들이라도 각각이 하나의 포인터이기 때문에 각노드를 이어붙이는 방식으로 접근하였다.
  
**개선된 dummy 방식 접근**  
![다운로드 (35)](https://github.com/user-attachments/assets/26507e92-3f15-4fa0-8a03-7f03c1f6d31b)  
다음 노드 주소에 이어붙일 노드 주소를 가리키게 하였다.  
  
**한 노드가 null일 경우 다른 노드를 전부 붙이는 이유**  
![다운로드 (36)](https://github.com/user-attachments/assets/8deb0b4a-c14b-428c-9964-3096a1d356e4)  
![다운로드 (32)](https://github.com/user-attachments/assets/6b9043b3-8425-40a6-8a08-c5be73680b08)  
그림이 모든 것을 말해준다. 굳이 비교할 필요가 없기 때문이다.  

## 2. 재귀 특성을 이용한 병합 방식
재귀 방식은 스택프레임을 이용한 호출 방식이다. 함수는 가장 마지막까지 호출되고 그 후에 맨 처음으로 되돌아가게 된다.  
그러나 노드 값을 반환하는 것만으로는 병합하는 부분이 보이지 않는다. 따라서 맨 처음으로 돌아갔을 때 어떻게 이어질 수가 있는 거지란 의문이 들었다.  

![다운로드 (33)](https://github.com/user-attachments/assets/b5ed38b5-f8ba-4255-a0d9-a89b87117800)  
핵심은 이전 호출 지점으로 되돌아갈 때 반환 값을 next로 이어주는 것이었다.  

# 코드  
```
// 더미(dummy) 방식으로 병합한 알고리즘 -> 개선 필요
public ListNode mergeTwoLists(ListNode list1, ListNode list2) {
        if (list1 == null && list2 == null) {
            return null;
        }
        
        ListNode answerNode = new ListNode();
        ListNode nextNode = answerNode;

        while(list1 != null || list2 != null) {
            if (list1 == null) {
                nextNode.val = list2.val;
                list2 = list2.next;
            } else if (list2 == null) {
                nextNode.val = list1.val;
                list1 = list1.next;
            } else {
                if (list1.val <= list2.val) {
                    nextNode.val = list1.val;
                    list1 = list1.next;
                } else {
                    nextNode.val = list2.val;
                    list2 = list2.next;
                }
            }

            if ( !(list1 == null && list2 == null) )
                nextNode = nextNode.next = new ListNode();
        }

        return answerNode;
    }
```
```
// dummy 방식 개선
public ListNode mergeTwoLists(ListNode list1, ListNode list2) {
        ListNode dummyNode = new ListNode();
        ListNode head = dummyNode;

        while (list1 != null && list2 != null) {
            if (list1.val <= list2.val) {
                head.next = list1;
                list1 = list1.next;
            } else {
                head.next = list2;
                list2 = list2.next;
            }

            head = head.next;
        }

        head.next = (list1 == null) ? list2 : list1;

        return dummyNode.next;
    }
```
```
// 재귀 접근
public ListNode mergeTwoLists(ListNode list1, ListNode list2) {
        if (list1 == null) {
            return list2;
        } else if (list2 == null) {
            return list1;
        }

        if (list1.val <= list2.val) {
            list1.next = mergeTwoLists(list1.next, list2);
            return list1;
        } else {
            list2.next = mergeTwoLists(list1, list2.next);
            return list2;
        }
    }
```